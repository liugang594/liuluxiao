<!DOCTYPE html>
<html>
  <head>
        <% include header2.ejs %>
  </head>
  <body>
    <p>以下为历次报名列表：</p>
    <% if(list){ %>
          <% for(var i=0; i<list.length; i++) {%>
              <a href="/weixin/baoming/history?date=<%=list[i]._id%>">活动日期：<%= list[i]._id %>，参加人数：<%= list[i]._count%></a><br />
          <% } %>
    <%}%>
  </body>
</html>
